## 2025-05-18 - [Empty State Pattern]
**Learning:** `v-data-table-virtual` default empty state is just text. Replacing it with `no-data` slot containing an icon (`mdi-cart-outline`) and instruction improves guidance significantly.
**Action:** Check other tables for `no-data-text` and suggest upgrading to `no-data` slot with icon.
